export const metadata = { title: "Contact — Burna Boy" };

export default function ContactPage() {
  return (
    <main>
      <header className="pageHeader container">
        <h1>
          Get in <span className="accent">Touch</span>
        </h1>
        <p>A contact form will live here — coming soon.</p>
      </header>
    </main>
  );
}
