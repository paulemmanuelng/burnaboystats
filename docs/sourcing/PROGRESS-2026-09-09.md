# Session state — 9 September 2026

Written mid-session against a credit limit. Everything below is either **shipped**
or **in flight**; nothing is speculative. Resume from "Open work" at the bottom.

## Shipped today (all merged to main)

| PR | What |
|---|---|
| #220 | An award ceremony was four days from stopping the stats bot; three guards were guarding nothing |
| #221 | Dai Dai Spotify run corrected — six days off the chart, not one |
| #222 | `next` 16.2.9 → 16.3.4 (cleared the critical advisory), dependabot config, enforced-header guard |
| #223 | The crown made findable by Google — stable favicon URL, ICO first, raster `Organization.logo` |

### #221 in detail, because it changed a founding assumption

`DAI_DAI_SPOTIFY_DAYS_OFF` read `1` from May until today. Paul's reading of the
**7 September** Global Daily chart — No. 11, streak 109, and in the expanded row
**110 days on chart** since the 15 May debut at No. 114 — forces `6`:
15 May → 7 Sep is 116 days, and 116 − 110 = 6.

The evidence was already in the repo and had been read backwards. `updates.ts`
carries six dated day-counts (82 on 10 Aug, 83, 86, 87, 90, 93) and every one is
exact as a **total** with six days off. `daiDai.ts` read them as **streaks**,
found them all one day ahead of Spotify's own Streak column, and pinned that gap
in `spotifyRun.test.ts` as a known transcription drift — a test asserting the
discrepancy instead of resolving it.

**Why nothing caught it:** the reconcile test had `DAYS_OFF` on both sides of its
own equation, so it balanced for any value. It now checks the six feed readings
with the constant on one side only, plus the total Spotify prints itself.

Published totals did not move: **37 days at No. 1**, counted through the 28 Aug
chart, now **closed** — Paul confirms the run ended. The anchors therefore split,
so the card prints two dates (`STREAK_READ_ON_LONG` = 7 Sep,
`NO1_READ_ON_LONG` = 28 Aug).

## Security audit — done, four dimensions

**No secrets** in 17,243 git objects across 95 branches (controls run first, so
the zeroes are real). **No exploitable vulnerability** in the app: no XSS sink,
no SSRF, no injection, no open redirect, no proxy route, no middleware.

**Still open — repo/account settings only Paul can change:**

1. Confirm **2FA** on GitHub (unverifiable from here — the local token lacks
   `user` scope) and on the recovery Gmail. Audit PATs + OAuth apps.
2. **Enable Dependabot alerts + security updates** — both off, which is why 11
   advisories accumulated unseen. Secret scanning is already on.
3. **Ruleset on `main`** — there is no branch protection at all, so "the bot
   drafts, a human merges" is convention rather than enforcement.
4. Turn off "Allow GitHub Actions to create and approve pull requests".
5. Vercel: check whether Attack Challenge Mode is on.

Verified clean and not worth re-litigating: default workflow token is `read`, no
`pull_request_target`, zero script-injection surface, no collaborators, no deploy
keys, no webhooks, no self-hosted runners, no SSH keys on the Mac, TLS 1.2+ only,
CAA records set. Googlebot gets 200 on `robots.txt`, `sitemap.xml` (113 URLs),
`rss.xml` and every page tested — an audit agent reported 403s there, but it had
tripped Vercel's rate limiter with rapid requests and read its own throttling as
a standing block.

## Recheck round one — APPLIED, PR #231

Workflow `wf_2ba2fd26-d3a` got 7 of 13 agents through before the session limit;
the rest were resumed from cache. Applied only what survived the adversarial pass:

- **France 6 → 9 weeks at No. 1.** SNEP's own weekly Top Singles, nine
  consecutive issues, semaine 28 (10 juillet) → semaine 36 (4 septembre), every
  row "La Semaine Dernière: 1er". The published 6 was the 14 août issue.
- **The YouTube run is NOT over.** charts.youtube.com's 7 Sep chart has Dai Dai
  at No. 1, Yesterday 2, 108 days on chart. Read twice. `DAI_DAI_YT_RUN_ENDED`
  is now false, both editions restated. **The 80 stays a CLOSED count** — the
  song was not No. 1 on 5 or 6 Sep and the new spell's first day is unread.
- **Mediatraffic feed wording** — 5 September is the chart WEEK, 30 August the
  issue date. Number right, label wrong. Both page surfaces were already correct.

### Deliberately NOT applied

- **France `weeks: 15`** — arithmetic correct, REFUTED on policy and rightly so.
  SNEP publishes no weeks-on-chart column, so France would be the only
  hand-counted run in `charts.ts`, and `/api/v1/charts` promises "peaks are read
  from each chart body's own run". **Paul's call:** keep France without `weeks`
  (status quo, consistent with the field's documented rule), or widen the rule
  and ship 15 with an explicit `note` saying it was counted from consecutive
  SNEP issues. If (b), the semaine 30 "DAI DAI (A CAPPELLA)" anomaly must be
  recorded and the /api/v1 description revisited.
- **Billboard GLB/GLBX weeks** — TollBit paywall returns "not authorized" to a
  real browser. Unverifiable; no figure moved on a guess.
- **Spotify Global Weekly "13th week" → 15th** — real, but its adversarial pass
  never ran. Next round.

## Still in flight — recheck round two

Workflow `wf_2ba2fd26-d3a`, launched 09:54. Eight clusters, each verified at the
chart body itself, then an adversarial pass over every proposed change, then a
completeness critic. **Nothing from it has been applied.**

Script:
`~/.claude/projects/-Users-paulemmanuelng-burnaboy-website/4773f091-3fa7-48cc-afc8-1748c3619fba/workflows/scripts/dai-dai-figure-recheck-wf_2ba2fd26-d3a.js`
Resume with `Workflow({scriptPath, resumeFromRunId: "wf_2ba2fd26-d3a"})` — completed
agents return cached results. Read `journal.jsonl` in the transcript dir before
assuming a cached result is non-empty.

Resumed for the six that hit the limit: `europe-other`, `europe-east-south`,
`americas-asia-mena`, `tallies-certs`, `refute:spotify-youtube`, and the
completeness critic. Done and cached: `france`, `germany`, `billboard-global`,
`spotify-youtube` (verify).

Germany came back **entirely clean** — peak 1, 10 weeks at No. 1, 15 weeks on
chart, all confirmed at offiziellecharts.de, and the "Shakira's second-longest
German No. 1, passing Waka Waka" claim verified exact.

### What prompted it, and the two findings that already stand

Paul asked how many weeks "Dai Dai" has spent in France and Germany. What the
site publishes today:

| | peak | weeks at No. 1 | weeks on chart |
|---|---|---|---|
| France (SNEP) | 1 | 6 | **absent** |
| Germany (GfK) | 1 | 10 | 15 |

1. **France publishes no weeks-on-chart figure at all.** It is one of only three
   countries with a run (FR, PA, NO) missing the `weeks` field, against 22 that
   have it. `weeksOnChart("Dai Dai", "FR")` returns `null`.
2. **Both are roughly a month stale, and undated.** The feed's last France
   reading is "a 5th week" (10 Aug); Germany's is "a 7th week at No. 1"
   (16 Aug). The published 6 and 10 are later reads than those, but nothing in
   the repo says when either was taken.

`SNEP` is JS-rendered but has permalinks and offers PDF/CSV export of a filtered
view — see `RESUME.md`. Germany is `offiziellecharts.de`.

## Open work, in order

1. **Apply the recheck results** once the workflow lands — Paul has asked for the
   fixes to be applied, not just reported. Expect the adversarial pass to reject
   some proposals; apply only what survives it, and only what a body actually
   confirmed. An unreadable register means omit.
2. Give France a `weeks` figure, or record why it cannot have one.
3. Date the France and Germany readings the way the Spotify figures now are.
4. The five security settings above.
5. Search Console: re-run VALIDATE FIX on both Events issues.
6. Mercedes rows — **do not delete**, awaiting Paul's chauffeur call.
7. `100vh` → `100dvh` in `globals.css:554` — offered, no decision yet.

## Two rules this session earned the hard way

- **A guard's negative control must be the literal string the site shipped.** A
  reverse guard written this morning was proved with `"and still counting"`; the
  page actually said `"— and back on top."`, which the detector could not see. It
  proved the detector detects itself. Pull the real line from git and keep it
  verbatim in the test.
- **A reconcile test with the same constant on both sides balances for any
  value.** Anchor to something the source publishes independently.
