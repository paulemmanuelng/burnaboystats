# Session state — 10 September 2026

Two threads. The first is finished and live; the second is mid-build on
`feat/cert-units-compare`.

---

## 1. Career Spotify total — DONE, MERGED, LIVE

**The site now publishes 10,956,307,905 ("10.96B")**, anchored to ChartMasters by
Paul's decision after a premium subscriber posted their dashboard. Merged as #242
and confirmed live on `/analysis/spotify-unmerge` and `/records/by-the-numbers`.

Done as an **offset move, not a typed string** — the bot rewrites those fields
daily, so a typed figure would have survived until its next run:

```
offset    80,606,612  ->  112,305,806     (same-date gap: CM 10,956,307,905 - kworb 10,844,002,099, both 9 Sep)
baseline  10,924,608,711  ->  10,956,307,905
```

**The corroboration, which came from the thread's own screenshot:** its board puts
Burna's 2026 gain at 1.756B, and `10,956,307,905 − 9,199,552,674` (the corrected
2025 close this site published on 21 Aug) `= 1,756,755,231`. That ties ChartMasters'
total, ChartMasters' board and the site's own analysis into one closing arithmetic —
and refutes the thread's central claim in passing, since a gain computed off the
pre-correction 9.58B would read 1.4B, not 1.756B.

**Maintenance, and it is a decay problem.** kworb's cumulative rises ~10.81M/day
against ~7.6M/day of real streaming as it absorbs catalogue it had been missing, so
`raw + 112,305,806` drifts ABOVE ChartMasters over time. Re-read ChartMasters and
re-subtract on the SAME date; never derive it from the published figure. The bot
cannot see the drift — 31M on 10.9B is 0.3%, under its 3% threshold.

Of the offset, **80,606,612 is measured** track by track and **31,699,194 is named
but not enumerated**. Do not summarise this file as a fully measured offset.

---

## 2. Certified-units comparison page — IN BUILD

A page comparing any two of sixteen artists by the units behind their plaques,
song vs song or catalogue vs catalogue. **1,212 plaques, 27 bodies, 16 artists.**

### Shipped on the branch

| | |
|---|---|
| `docs/design/CERT-UNITS-COMPARE-SPEC.md` | every decision, magnitudes, states, entry points |
| `docs/sourcing/CERT-THRESHOLDS.md` / `.json` | 27 bodies, every source URL and quote |
| `docs/sourcing/CERT-THRESHOLDS-WORKLIST.md` | how the 27-country scope was derived |
| `app/data/certThresholds.ts` | the table, generated from the research |
| `app/lib/certUnits.ts` | the engine |
| `tests/certUnits.test.ts` | 29 tests |

**Design handoff (live, shareable):**
https://claude.ai/code/artifact/92546027-dc37-4eeb-8212-3aca8cd24753

### Decisions locked (Paul)

- **Certified units, never "units sold."** A plaque is a FLOOR.
- Features: toggle, **default off**.
- Nigeria: **separated, never deleted**. Opens ON for **57 of 120 pairs** —
  `(both artists >=50% Nigerian) OR (either side has zero international plaques)`.
  A third clause for thin-but-not-empty pairs is **provably redundant**: 0 pairs.
- Song select shows cover + name + artist names; artist select shows both images.
- Compare button on **every** artist's certifications page, stat card moves to
  secondary rather than being removed.
- All 27 countries researched before launch. No partial ship.
- **Any pair, no home team.** Not Burna vs a challenger.

### The four engine rules, each with a live counter-example

1. **Never sum a release's own awards.** Zero duplicates in the data today, so it
   never fires — but `certHistory` is an append-only event log and anything built
   off that would roughly double every figure.
2. **A multiplier rides whatever tier it sits on.** Tyla's "Water" is 2x DIAMOND
   in Brazil, the only such case in 1,212 plaques.
3. **What cannot be priced is counted and named.** 35 plaques sit in a
   country/format with no usable threshold.
4. **An award PROGRAMME overrides the country's scale.** RIAA Latin certifies a
   Platino at 60,000 against the standard programme's 1,000,000. **This was a real
   bug I shipped and caught:** the first engine ignored `cert.body` and priced
   "Dai Dai"'s US 2x Platino at 2,000,000 instead of 120,000.

### RESUME HERE

**A RIAA sweep was running when this was written** (`wf_b4294428-9df`), checking 10
large US plaques for Latin-programme awards, with any "latin" verdict sent to an
adversarial re-check.

1. **Tag Ayra Starr's "Santa" if the sweep confirms Latin.** `AfroCert` already
   has the `body` field; add `body: "RIAA Latin"` beside its US cert exactly as
   `certifications.ts` does for "Dai Dai". Until then **Ayra Starr shows
   18,103,333 and falsely tops the board** — 16,000,000 of it that one plaque,
   priced at the standard programme's 1,000,000 per multiple instead of 60,000.
   Rema's "Bubalu" (2x Platinum, Spanish-language) is the other prime suspect.
2. **Cover art: 73 of Burna's 85 certified releases have none.** The board is
   656/662 covered. Gbona vs Essence would render Essence with art and Gbona
   without, on Burna Boy's own site. Mechanical: board covers are Spotify CDN
   URLs. Needs a `cover` field on `Release` in `certifications.ts`.
3. **`AfroRelease` has no `credit` field**, so a board artist's featured
   appearances cannot say whose record they appear on.
4. Then the page itself, and the button swap on both layouts.

### Process note worth keeping

`npm run verify | grep ...` **cannot fail** — grep exits 0 on a red run, and a
commit chained behind it pushed a failing suite. Capture the exit code:
`npm run verify > log 2>&1; echo $?`. The two failures that time were timeout
flakiness under parallel load, not a real break (a clean re-run gave 904 passing),
but the check was worthless either way.
