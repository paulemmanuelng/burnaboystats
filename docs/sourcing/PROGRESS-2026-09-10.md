# Session state — 10 September 2026

Two threads. The first is finished and live; the second is mid-build on
`feat/cert-units-compare`.

---

## 1. Career Spotify total — DONE, MERGED, LIVE

**The site now publishes 10,956,307,905 ("10.96B")**, anchored to ChartMasters by
Paul's decision after a premium subscriber posted their dashboard. Merged as #242
and confirmed live on `/analysis/spotify-unmerge` and `/records/by-the-numbers`.

Done as an **offset move, not a typed string** — the bot rewrites those fields
daily, so a typed figure would have survived until its next run:

```
offset    80,606,612  ->  112,305,806     (same-date gap: CM 10,956,307,905 - kworb 10,844,002,099, both 9 Sep)
baseline  10,924,608,711  ->  10,956,307,905
```

**The corroboration, which came from the thread's own screenshot:** its board puts
Burna's 2026 gain at 1.756B, and `10,956,307,905 − 9,199,552,674` (the corrected
2025 close this site published on 21 Aug) `= 1,756,755,231`. That ties ChartMasters'
total, ChartMasters' board and the site's own analysis into one closing arithmetic —
and refutes the thread's central claim in passing, since a gain computed off the
pre-correction 9.58B would read 1.4B, not 1.756B.

**Maintenance, and it is a decay problem.** kworb's cumulative rises ~10.81M/day
against ~7.6M/day of real streaming as it absorbs catalogue it had been missing, so
`raw + 112,305,806` drifts ABOVE ChartMasters over time. Re-read ChartMasters and
re-subtract on the SAME date; never derive it from the published figure. The bot
cannot see the drift — 31M on 10.9B is 0.3%, under its 3% threshold.

Of the offset, **80,606,612 is measured** track by track and **31,699,194 is named
but not enumerated**. Do not summarise this file as a fully measured offset.

---

## 2. Certified-units comparison page — IN BUILD

A page comparing any two of sixteen artists by the units behind their plaques,
song vs song or catalogue vs catalogue. **1,212 plaques, 27 bodies, 16 artists.**

### Shipped on the branch

| | |
|---|---|
| `docs/design/CERT-UNITS-COMPARE-SPEC.md` | every decision, magnitudes, states, entry points |
| `docs/sourcing/CERT-THRESHOLDS.md` / `.json` | 27 bodies, every source URL and quote |
| `docs/sourcing/CERT-THRESHOLDS-WORKLIST.md` | how the 27-country scope was derived |
| `app/data/certThresholds.ts` | the table, generated from the research |
| `app/lib/certUnits.ts` | the engine |
| `tests/certUnits.test.ts` | 29 tests |

**Design handoff (live, shareable):**
https://claude.ai/code/artifact/92546027-dc37-4eeb-8212-3aca8cd24753

### Decisions locked (Paul)

- **Certified units, never "units sold."** A plaque is a FLOOR.
- Features: toggle, **default off**.
- Nigeria: **separated, never deleted**. Opens ON for **57 of 120 pairs** —
  `(both artists >=50% Nigerian) OR (either side has zero international plaques)`.
  A third clause for thin-but-not-empty pairs is **provably redundant**: 0 pairs.
- Song select shows cover + name + artist names; artist select shows both images.
- Compare button on **every** artist's certifications page, stat card moves to
  secondary rather than being removed.
- All 27 countries researched before launch. No partial ship.
- **Any pair, no home team.** Not Burna vs a challenger.

### The four engine rules, each with a live counter-example

1. **Never sum a release's own awards.** Zero duplicates in the data today, so it
   never fires — but `certHistory` is an append-only event log and anything built
   off that would roughly double every figure.
2. **A multiplier rides whatever tier it sits on.** Tyla's "Water" is 2x DIAMOND
   in Brazil, the only such case in 1,212 plaques.
3. **What cannot be priced is counted and named.** 35 plaques sit in a
   country/format with no usable threshold.
4. **An award PROGRAMME overrides the country's scale.** RIAA Latin certifies a
   Platino at 60,000 against the standard programme's 1,000,000. **This was a real
   bug I shipped and caught:** the first engine ignored `cert.body` and priced
   "Dai Dai"'s US 2x Platino at 2,000,000 instead of 120,000.

### BUILT SINCE (evening, 10 Sep)

`/compare` is **live on the branch and working**, with all three entry points.
922 tests passing, verify exit 0. Commits `6a52c46` → `34caca1`.

| | |
|---|---|
| `app/compare/page.tsx` + `compare.module.css` | the page |
| `app/compare/opengraph-image.tsx` | its share card, counts derived |
| `app/methodology/page.tsx` | new `#certified-units` section, four rules |
| engine | listed-not-counted rows, caveats, sort, Nigeria pin, collapse |
| entry points | desktop + mobile certs, desktop + mobile board artist |

**Server-rendered off the query string, not a client app.** Every comparison is a
URL; the plaque corpus and threshold table stay out of the browser bundle; it
renders complete without JavaScript. A toggle is a navigation — worth it.

**One responsive tree, not two components.** The design's mobile frames are the
desktop parts restacked with shorter labels, so two components would be two
copies of one design — the drift the separate-layouts rule exists to prevent
rather than an instance of it. Reasoning is in the stylesheet header.

It reproduces the design's fixtures to the unit: **Gbona 915,333 vs Essence
6,340,000, union 10 rows, 7 of 8 plaques counted vs 7 of 7, Nigeria included
6,540,000.**

### Data corrections landed today

- **"Do I" is Phyno & Burna Boy**, not a Burna lead — confirmed at TCSN's own
  register and Spotify. It sat uncredited, which read as a solo record. Every
  lead entry was then cross-checked against kworb's feature asterisks: exactly 2
  of 60 are asterisked, Dai Dai and Do I, and both now state their credit.
- **Ayra Starr "Santa" and Rema "Bubalu" are RIAA LATIN** — 960,000 and 120,000,
  not 16,000,000 and 2,000,000. All 16 artists were then swept on RIAA's Latin
  tab: exactly three Latin awards exist board-wide and all three are tagged.
- **Cover art: 12 of 85 → 82 of 85.** Three left empty on purpose.
- **The two US Diamonds STAY Diamond** — Paul's call, with the trade recorded.

### IN FLIGHT WHEN THIS WAS WRITTEN — check both before continuing

Two workflows were still running. **Their results are not in the repo yet.**

1. **`wf_82fbcd35-791` — threshold deep hunt.** Seven bodies whose plaques cannot
   be priced (SE, PL, GR, BE, CO, CZ, SK), searched beyond their own sites:
   archives, IFPI's consolidated levels list, national PDFs. Every figure graded
   A (body's own site) / B (its archive) / C (IFPI's list) / D (secondary only).
   **Apply A–C only.** D is not publishable here and must not be dressed up as
   sourced. Note SE and PL are *conversion* problems, not absence — both publish
   thresholds, in capped streams and in PLN respectively.
2. **`wf_47464b73-dcd` — adversarial audit of every /compare mechanism.** Seven
   lenses, each defect re-tested by a second agent before it counts. The
   arithmetic lens writes its OWN pricing pass from CERT-THRESHOLDS.json rather
   than reading the engine, so it can actually disagree.

Two defects I expect them to find, recorded so a fresh reader can check quickly:
the difference **ratio when one side is zero** (Burna vs Black Sherif), and
**footnote ¹ naming bodies whose rows are hidden inside the collapse**, so the
marker points at a row you cannot see.

An untracked `tests/_audit_*.test.ts` may be left behind by an audit agent —
delete it, it is scratch.

### 11 Sep — both audits landed and were applied

- **Threshold hunt:** Belgium (B), Czechia (A), Slovakia (B) found, verified and
  applied. Burna's not-comparable plaques 13 → 10. Greece is grade D; Colombia has
  no singles threshold at any grade; Sweden and Poland confirmed unconvertible AT
  THE BODY. Full table in CERT-THRESHOLDS.md.
- **Compare audit:** crashed in post-processing on the usage limit, but six lenses
  had finished and all eleven recovered verdicts were real. ~26 defects fixed and
  verified live — Brazil on the wrong table, Essence priced two ways, BNXN with no
  Nigeria rescue, daggers on the wrong plaques, Sweden's five hidden singles, ties
  announced as leads, a record compared with itself. Commits `…` on the branch.
- **Lede example is now a UK Platinum single**, per Paul.

### 11 Sep, later — both open questions resolved by agents, per Paul

1. **"Ginger" in Switzerland is GOLD.** Three routes into the Swiss register
   (swisscharts.com Edelmetall lists 2020–26, its search, Wayback 2024 and 2026)
   all print one row: Gold, 2023. Burna's Platinum was wrong; corrected in the
   release entry, certHistory, updates.ts and the corrections ledger. Visible:
   /certifications tier split 101/96 → **100 Platinum / 97 Gold**.
2. **The floor rule is APPLIED** — see CERT-THRESHOLDS.md "The floor rule —
   APPLIED". Extract → verify → max-effort red team; three breaks folded in.
3. The staleness alarm on `spotify-streams-*` is date-driven; the bot clears it.

### STILL NEEDS PAUL

- Merge `feat/cert-units-compare` to main.

### RESUME HERE

**THE RIAA SWEEP RAN AND RETURNED NOTHING.** `wf_b4294428-9df`: all 10 agents died
on a session limit (resets 20:00 Europe/London, 10 Sep) before reaching riaa.com.
Zero result lines in its journal — there is no partial data to salvage.

**Read that as "not checked", never as "checked and clean."** No US plaque on this
roster has been verified against RIAA's own database. The 10 records queued were:
Santa (Ayra Starr), Bubalu (Rema), love nwantiti (CKay), Calm Down (Rema), Essence
(Wizkid), Essence (Tems), Water (Tyla), One Dance (Wizkid), Wait For U (Tems), Free
Mind (Tems). Re-run the same workflow after the limit resets:

```
Workflow({scriptPath: ".../riaa-latin-sweep-wf_b4294428-9df.js", resumeFromRunId: "wf_b4294428-9df"})
```

Nothing was tagged, and nothing should be until the body confirms it.

1. **Tag Ayra Starr's "Santa" if the sweep confirms Latin.** `AfroCert` already
   has the `body` field; add `body: "RIAA Latin"` beside its US cert exactly as
   `certifications.ts` does for "Dai Dai". Until then **Ayra Starr shows
   18,103,333 and falsely tops the board** — 16,000,000 of it that one plaque,
   priced at the standard programme's 1,000,000 per multiple instead of 60,000.
   Rema's "Bubalu" (2x Platinum, Spanish-language) is the other prime suspect.
2. **Cover art: 73 of Burna's 85 certified releases have none.** The board is
   656/662 covered. Gbona vs Essence would render Essence with art and Gbona
   without, on Burna Boy's own site. Mechanical: board covers are Spotify CDN
   URLs. Needs a `cover` field on `Release` in `certifications.ts`.
3. **`AfroRelease` has no `credit` field**, so a board artist's featured
   appearances cannot say whose record they appear on.
4. Then the page itself, and the button swap on both layouts.

### Process note worth keeping

`npm run verify | grep ...` **cannot fail** — grep exits 0 on a red run, and a
commit chained behind it pushed a failing suite. Capture the exit code:
`npm run verify > log 2>&1; echo $?`. The two failures that time were timeout
flakiness under parallel load, not a real break (a clean re-run gave 904 passing),
but the check was worthless either way.
